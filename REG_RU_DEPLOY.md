# Инструкция по деплою на REG.Ru

## 🚀 Быстрый деплой (Автоматический)

Если у вас уже настроены FTP-данные, просто выполните:

```bash
npm run deploy
```

Эта команда автоматически:
1. Соберет проект (`npm run build`)
2. Загрузит все файлы на сервер через FTP

**FTP-данные уже настроены в скрипте `deploy-ftp.js`**

---

## Подготовка файлов

1. **Соберите проект:**
   ```bash
   npm run build
   ```
   
2. **Проверьте папку `dist/`** - там должны быть все файлы для загрузки:
   - `index.html`
   - `assets/` (CSS, JS, изображения)
   - `favicon.svg`
   - `robots.txt`
   - `sitemap.xml`
   - `.htaccess`
   - PDF файлы (если есть)

## Способ 1: Автоматическая загрузка через скрипт (Рекомендуется)

### Использование готового скрипта:

```bash
npm run deploy
```

Скрипт автоматически:
- Соберет проект
- Подключится к FTP серверу
- Загрузит все файлы из `dist/` в `public_html/`

### Настройка FTP-данных:

FTP-данные уже настроены в файле `deploy-ftp.js`. Если нужно изменить:

1. Откройте `deploy-ftp.js`
2. Измените значения в `FTP_CONFIG`:
   ```javascript
   const FTP_CONFIG = {
     host: '31.31.196.49',
     user: 'u3382604',
     password: 'ваш_пароль',
     secure: false,
     port: 21
   };
   ```

**Или используйте переменные окружения** (более безопасно):

```bash
# Windows PowerShell
$env:FTP_HOST="31.31.196.49"
$env:FTP_USER="u3382604"
$env:FTP_PASSWORD="ваш_пароль"
npm run deploy

# Linux/Mac
FTP_HOST=31.31.196.49 FTP_USER=u3382604 FTP_PASSWORD=ваш_пароль npm run deploy
```

## Способ 2: Загрузка через FTP-клиент (FileZilla)

### Шаг 1: Подключитесь через FileZilla

1. Скачайте FileZilla: https://filezilla-project.org/
2. Установите и откройте
3. Введите данные FTP:
   - **Хост:** `31.31.196.49`
   - **Имя пользователя:** `u3382604`
   - **Пароль:** `pOuePd0d3Wu96FjA`
   - **Порт:** `21`
4. Нажмите "Быстрое соединение"

### Шаг 2: Загрузите файлы

1. **Удалите все старые файлы** из папки `public_html` (если есть)
2. **Загрузите ВСЕ файлы из папки `dist/`** в `public_html`:
   - Скопируйте `index.html` в корень `public_html`
   - Скопируйте всю папку `assets/` в `public_html/assets/`
   - Скопируйте `favicon.svg` в корень `public_html`
   - Скопируйте `robots.txt` в корень `public_html`
   - Скопируйте `sitemap.xml` в корень `public_html`
   - Скопируйте `.htaccess` в корень `public_html`
   - Скопируйте PDF файлы в корень `public_html`

### Шаг 3: Проверьте структуру

После загрузки структура должна быть такой:
```
public_html/
├── index.html
├── favicon.svg
├── robots.txt
├── sitemap.xml
├── .htaccess
├── politika_obrabotki_i_zashiti_dannih.pdf
├── oferta_2.pdf
└── assets/
    ├── index-*.css
    ├── index-*.js
    └── *.svg (иконки и логотипы)
```

## Способ 3: Через панель REG.Ru (Файловый менеджер)

1. В панели REG.Ru перейдите в **"Файловый менеджер"**
2. Откройте папку `public_html` или `www` (корневая папка сайта)
3. Загрузите все файлы из папки `dist/` через веб-интерфейс

## Настройка домена

### Шаг 1: Подключение домена к хостингу

1. В панели REG.Ru перейдите в **"Домены"** → **"Управление DNS"**
2. Убедитесь, что DNS записи указывают на ваш хостинг:
   - **A-запись**: `@` → `31.31.196.49` (IPv4)
   - **AAAA-запись**: `@` → `2a00:f940:2:2:1:1:0:275` (IPv6)
   - **CNAME**: `www` → `@` или IP-адрес

### Шаг 2: Настройка SSL-сертификата

1. В панели REG.Ru найдите раздел **"SSL-сертификаты"**
2. Активируйте бесплатный Let's Encrypt сертификат для домена `takto.space`
3. Включите принудительное перенаправление на HTTPS (или используйте `.htaccess` файл)

**Важно:** Файл `.htaccess` уже настроен для перенаправления HTTP → HTTPS

## Важные настройки

### 1. Файл .htaccess (для Apache)

Файл `.htaccess` уже создан и автоматически копируется в `dist/` при сборке. Он включает:
- ✅ Перенаправление на HTTPS
- ✅ Поддержка SPA (React Router)
- ✅ Кэширование статических файлов
- ✅ Сжатие файлов

### 2. Проверка путей к файлам

Убедитесь, что в `index.html` все пути начинаются с `/`:
- `/favicon.svg` ✅
- `/assets/...` ✅
- `/robots.txt` ✅

## Проверка после деплоя

1. **Откройте сайт:** https://takto.space
2. **Проверьте:**
   - ✅ Сайт открывается
   - ✅ Все стили загружаются
   - ✅ Все изображения отображаются
   - ✅ Форма работает
   - ✅ Яндекс Метрика работает (проверьте в консоли браузера)
   - ✅ PDF файлы открываются
   - ✅ HTTPS работает (замочек в адресной строке)

3. **Проверьте консоль браузера** (F12) на наличие ошибок

## Обновление сайта

После каждого изменения:

### Вариант 1: Автоматический (рекомендуется)
```bash
npm run deploy
```

### Вариант 2: Вручную
1. Соберите проект: `npm run build`
2. Загрузите обновленные файлы из `dist/` через FTP или файловый менеджер

## Полезные ссылки

- Панель управления REG.Ru: https://www.reg.ru/user/
- Документация REG.Ru: https://www.reg.ru/support/
- Файловый менеджер: доступен в панели управления

## Поддержка

Если возникли проблемы:
1. Проверьте логи в панели REG.Ru
2. Убедитесь, что все файлы загружены правильно
3. Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)
4. Проверьте, что `.htaccess` загружен в корень `public_html`
5. Обратитесь в поддержку REG.Ru

## Безопасность

⚠️ **Важно:** Файл `deploy-ftp.js` содержит FTP-пароль. Не коммитьте его в публичный репозиторий!

Для безопасности используйте переменные окружения (см. раздел "Настройка FTP-данных" выше).
