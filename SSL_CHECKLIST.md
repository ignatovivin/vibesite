# Чеклист проверки SSL сертификата

## ✅ Что уже настроено:

1. ✅ **Редирект на HTTPS** - настроен в `.htaccess`
2. ✅ **Все URL используют HTTPS** - обновлены в коде
3. ✅ **Canonical URL** - `https://takto.space`
4. ✅ **Open Graph мета-теги** - используют HTTPS
5. ✅ **JSON-LD структурированные данные** - используют HTTPS
6. ✅ **Sitemap.xml** - все URL используют HTTPS
7. ✅ **Robots.txt** - sitemap URL использует HTTPS

## 🔍 Проверка SSL сертификата:

### 1. Проверка в браузере:

1. Откройте https://takto.space
2. Проверьте адресную строку:
   - ✅ Должен быть **замочек** (🔒) в адресной строке
   - ✅ Должен быть **HTTPS** в URL
   - ✅ Не должно быть предупреждений о незащищенном соединении

3. Нажмите на замочек → **"Соединение защищено"**
4. Проверьте информацию о сертификате:
   - ✅ Должен быть указан **"Let's Encrypt"** или другой доверенный центр
   - ✅ Должна быть дата окончания действия
   - ✅ Должен быть статус **"Действителен"**

### 2. Проверка редиректа HTTP → HTTPS:

1. Откройте http://takto.space (без S)
2. Должно произойти автоматическое перенаправление на https://takto.space
3. В адресной строке должен появиться HTTPS

### 3. Онлайн проверка SSL:

Используйте онлайн-сервисы для проверки:

- **SSL Labs**: https://www.ssllabs.com/ssltest/analyze.html?d=takto.space
  - Ожидаемый результат: **A** или **A+** (отличная оценка)

- **SSL Shopper**: https://www.sslshopper.com/ssl-checker.html#hostname=takto.space
  - Проверяет срок действия сертификата
  - Проверяет цепочку сертификатов

### 4. Проверка через командную строку:

```bash
# Windows PowerShell
openssl s_client -connect takto.space:443 -servername takto.space

# Linux/Mac
openssl s_client -connect takto.space:443 -servername takto.space
```

## ⚠️ Возможные проблемы:

### Проблема 1: Смешанный контент (Mixed Content)

Если на HTTPS странице загружаются HTTP ресурсы, браузер покажет предупреждение.

**Решение:**
- Убедитесь, что все ссылки используют `https://` или относительные пути
- Проверьте консоль браузера (F12) на наличие ошибок "Mixed Content"

### Проблема 2: Сертификат не доверенный

Если браузер показывает предупреждение о недоверенном сертификате:

**Решение:**
- Проверьте, что сертификат установлен правильно
- Убедитесь, что это Let's Encrypt или другой доверенный центр
- Подождите 5-15 минут после установки (распространение сертификата)

### Проблема 3: Редирект не работает

Если HTTP не перенаправляется на HTTPS:

**Решение:**
1. Проверьте, что `.htaccess` загружен на сервер
2. Проверьте, что в панели REG.Ru включено перенаправление на HTTPS
3. Проверьте права доступа к файлу `.htaccess` (должны быть 644)

## 📋 Что проверить после установки SSL:

- [ ] Сайт открывается по HTTPS
- [ ] В адресной строке есть замочек
- [ ] HTTP автоматически перенаправляется на HTTPS
- [ ] Нет ошибок "Mixed Content" в консоли
- [ ] Сертификат действителен и не истек
- [ ] Все ресурсы (CSS, JS, изображения) загружаются по HTTPS

## ✅ Итоговая проверка:

После установки SSL сертификата:

1. ✅ Сайт доступен по HTTPS
2. ✅ Браузер показывает зеленый замочек
3. ✅ Все HTTP запросы перенаправляются на HTTPS
4. ✅ Сайт безопасен для пользователей
5. ✅ SEO улучшится (Google предпочитает HTTPS сайты)

## 🎉 Поздравляем!

Если все проверки пройдены успешно, ваш сайт полностью защищен и готов к работе!
